// Bar zone reads only from i18n. (Whether it renders is gated by states.showBar
// in Lab.jsx.)
export const get_barZoneProps = (states, handlers, childComps, t) => ({
  states: {},
  handlers: {},
  childComps: {},
  t,
});
