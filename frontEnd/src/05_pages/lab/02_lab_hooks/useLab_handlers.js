/**
 * Lab page handlers. The page has none today (the games own their own state),
 * so this returns an empty bag — kept for parity with the page pattern.
 */
export const useLab_handlers = () => {
  return {
    handlers: {},
  };
};
