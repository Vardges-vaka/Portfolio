// Skills section renders the radar ⇄ graph panel with the derived radar data.
export const get_skillsProps = (states, handlers, childComps, t) => ({
  states: { radarData: states.radarData },
  handlers: {},
  childComps: {},
  t,
});
