// Lab teaser reads only from i18n. (Whether it renders at all is gated by the
// isLabsEnabled flag in Tech.jsx.)
export const get_labTeaserProps = (states, handlers, childComps, t) => ({
  states: {},
  handlers: {},
  childComps: {},
  t,
});
