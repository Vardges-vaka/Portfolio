/**
 * Tech page UI state. The page has no local toggles (everything is derived from
 * i18n + data), so the bags are empty — kept for parity with the page pattern.
 */
export const useTech_states = () => {
  return {
    states: {},
    setters: {},
    refs: {},
  };
};
