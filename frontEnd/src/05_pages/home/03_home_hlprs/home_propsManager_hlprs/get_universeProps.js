// Universe section reads only from i18n (the graph pulls its own data).
export const get_universeProps = (states, handlers, childComps, t) => ({
  states: {},
  handlers: {},
  childComps: {},
  t,
});
