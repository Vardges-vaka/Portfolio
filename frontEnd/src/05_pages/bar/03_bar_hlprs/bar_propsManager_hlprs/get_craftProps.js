// Craft section feeds the radar with the bar skill set.
export const get_craftProps = (states, handlers, childComps, t) => ({
  states: { skills: states.skills },
  handlers: {},
  childComps: {},
  t,
});
