// Hero needs the scroll-to-contact handler.
export const get_heroProps = (states, handlers, childComps, t) => ({
  states: {},
  handlers: { scrollToContact: handlers.scrollToContact },
  childComps: {},
  t,
});
