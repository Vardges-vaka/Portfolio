/**
 * Bar page UI state. No local toggles — the bags stay empty (kept for parity
 * with the page pattern).
 */
export const useBar_states = () => {
  return {
    states: {},
    setters: {},
    refs: {},
  };
};
